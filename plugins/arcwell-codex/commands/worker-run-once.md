---
description: Drain queued local worker jobs once
argument-hint: [max-jobs=10]
---

# Worker Run Once

The user invoked this command with: $ARGUMENTS

Run the local worker once with a bounded job count. Report completed, failed, and dead-lettered counts, then inspect jobs if failures occurred.
