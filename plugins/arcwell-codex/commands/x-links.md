---
description: List indexed local X URL occurrences
argument-hint: "[--query TEXT] [--limit N]"
---

# X Links

The user invoked this command with: $ARGUMENTS

Use `x_links`. List URL occurrences that were already extracted from local X tweets. Do not fetch, expand, open, crawl, or summarize the linked URLs unless the user explicitly asks for a separate URL-ingest or web-research action.
