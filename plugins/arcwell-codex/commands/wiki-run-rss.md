---
description: Run an RSS or Atom adapter fetch immediately
argument-hint: [feed-url]
---

# Wiki Run RSS

The user invoked this command with: $ARGUMENTS

Use `arcwell wiki run-rss`. Run immediately only when requested; otherwise prefer `/watch-rss` to enqueue a normal job.
