---
description: Create a local arcwell backup
argument-hint: []
---

# Backup Create

The user invoked this command with: $ARGUMENTS

Use `backup_create`. Report the backup path and the manifest's X recovery summary, including whether canonical X rows exist, whether a portable X bundle is missing/fresh/stale/failed, and that portable bundles are separate unless explicitly exported and stored.
