---
description: Import authenticated X following accounts into watch sources
argument-hint: [max-users=1000]
---

# X Import Following Watch Sources

The user invoked this command with: $ARGUMENTS

Use `x_import_following_watch_sources`. Use this only when the user explicitly asks for following import; prefer `x_rebuild_definitive_watch_sources` for the curated bookmark/recent-follow watch list.
