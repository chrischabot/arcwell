---
description: List wiki and source adapter jobs
argument-hint: [optional-filter]
---

# Wiki Jobs

The user invoked this command with: $ARGUMENTS

Use `wiki_jobs`. Highlight queued, running, failed, retrying, and dead-lettered jobs. If failures exist, include the most actionable error messages.
