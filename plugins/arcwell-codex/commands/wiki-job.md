---
description: Inspect one wiki job
argument-hint: [job-id]
---

# Wiki Job

The user invoked this command with: $ARGUMENTS

Use `wiki_job_status`. Report status, attempts, timestamps, source kind, error text, and any output ids.
