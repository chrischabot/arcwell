---
description: List or apply reviewable memory candidates
argument-hint: [list|apply ID]
---

# Memory Candidates

The user invoked this command with: $ARGUMENTS

Use `candidate_list` by default. If the user supplied `apply ID`, use `candidate_apply`. Do not apply sensitive or ambiguous candidates without making the final stored text explicit.
