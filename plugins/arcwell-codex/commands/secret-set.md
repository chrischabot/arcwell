---
description: Store a local SQLite-backed secret value
argument-hint: NAME= VALUE= [SCOPE=]
---

# Secret Set

The user invoked this command with: $ARGUMENTS

Use `secret_value_set` only when the user supplied an explicit secret name and value. Do not echo the value back. Confirm the secret name and scope after storage.
