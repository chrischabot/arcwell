---
description: Store an external secret reference
argument-hint: NAME= LOCATION= SCOPE= [EXPIRES_AT=]
---

# Secret Ref Set

The user invoked this command with: $ARGUMENTS

Use `arcwell secrets set-ref`. Store a reference to where the secret lives, not the secret value itself. Do not echo secret material into chat.
