---
description: Run a GitHub adapter fetch immediately
argument-hint: OWNER[/REPO] [MODE=releases] [LIMIT=10]
---

# Wiki Run GitHub

The user invoked this command with: $ARGUMENTS

Use `arcwell wiki run-github-owner` for owners or `arcwell wiki run-github` for repositories. Run immediately only when requested; otherwise prefer `/watch-github`.
