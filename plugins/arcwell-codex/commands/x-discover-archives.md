---
description: Find likely local Twitter/X archives without importing them
argument-hint: [--dir path] [--limit N]
---

# X Discover Archives

The user invoked this command with: $ARGUMENTS

Use `x_discover_archives`. This is a no-write discovery command: do not import,
fetch, parse tweet bodies, read secrets, or claim completeness from discovery
alone. Report candidate paths, score, supported slices inferred from names or
shallow ZIP member names, and warnings such as unsafe members or shallow scans.
