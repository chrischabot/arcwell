---
description: Queue an arXiv query watch fetch
argument-hint: [query]
---

# Watch arXiv

The user invoked this command with: $ARGUMENTS

Use `wiki_enqueue_arxiv`. Queue an arXiv search job for the supplied query, such as `cat:cs.AI`. Run `worker_run_once` only when immediate processing is requested.
