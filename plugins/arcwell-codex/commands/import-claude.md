---
description: Analyze or import a Claude conversation export
argument-hint: PATH= [--dry-run] [--write-candidates]
---

# Import Claude

The user invoked this command with: $ARGUMENTS

Use `arcwell import claude`. Default to dry-run analysis unless the user explicitly asks to write candidates. When writing, create reviewable memory/profile candidates rather than silently applying them.
