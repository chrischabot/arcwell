---
description: Delete one profile item by key
argument-hint: [profile-key]
---

# Profile Delete

The user invoked this command with: $ARGUMENTS

Use `arcwell profile delete` for the supplied key. Confirm the exact key before deletion if the user's request is ambiguous.
