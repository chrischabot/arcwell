---
description: Record a model or provider cost entry
argument-hint: PACKAGE= JOB_ID= PROVIDER= MODEL= [ESTIMATED_USD=] [ACTUAL_USD=]
---

# Cost Add

The user invoked this command with: $ARGUMENTS

Use the local CLI command `arcwell cost add` with the supplied package, job id, provider, model, and cost values. Do not invent missing costs.
