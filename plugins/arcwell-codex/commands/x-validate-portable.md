---
description: Validate a portable Arcwell X bundle before import
argument-hint: [dir]
---

# X Validate Portable

The user invoked this command with: $ARGUMENTS

Use `x_validate_portable`. Treat the bundle as untrusted local input. Validate
the manifest format and version, safe shard paths, SHA-256 hashes, UTF-8 JSONL,
row counts, and token-like content checks before recommending import.
