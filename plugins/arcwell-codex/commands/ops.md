---
description: Show arcwell health and operations snapshot
argument-hint: [optional-focus]
---

# Ops

The user invoked this command with: $ARGUMENTS

Use `ops_snapshot`. Highlight health warnings, pending/failed/dead-lettered jobs, cursor state, edge events, projects, digest candidates, and watch source counts.
