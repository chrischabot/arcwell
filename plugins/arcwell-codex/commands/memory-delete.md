---
description: Delete one personal memory by id
argument-hint: [memory-id]
---

# Memory Delete

The user invoked this command with: $ARGUMENTS

Use `arcwell memory delete` for the supplied id. Confirm the exact memory id before deletion if the user's request is ambiguous.
