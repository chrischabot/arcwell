---
description: Add or review a personal memory fact
argument-hint: [fact]
---

# Remember

The user invoked this command with: $ARGUMENTS

Add the supplied fact to personal memory using `memory_add` when it is clearly a stable personal fact or preference. If the text is ambiguous, sensitive, or extracted from a larger source, use `memory_extract_candidates` so it can be reviewed before applying.

Keep personal memory separate from wiki knowledge.
