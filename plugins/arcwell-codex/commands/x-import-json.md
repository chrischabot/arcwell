---
description: Import replayed X items from a JSON file
argument-hint: [path]
---

# X Import JSON

The user invoked this command with: $ARGUMENTS

Use `x_import_json_file`. Report imported item count, source-card/page ids, and rejected records.
