---
description: Show latest local backup status
argument-hint: []
---

# Backup Status

The user invoked this command with: $ARGUMENTS

Use the local CLI command `arcwell backup status`. Report the latest backup path, timestamp, and verification status if available.
