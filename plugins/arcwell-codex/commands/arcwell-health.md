---
description: Check arcwell local health
argument-hint: [optional-focus]
---

# Agent Health

The user invoked this command with: $ARGUMENTS

Use `arcwell_health` in Codex, equivalent to `arcwell health` in a shell.
Report database, wiki, job, and configuration health plainly. If a focus was
supplied, lead with that subsystem.
