---
description: List local wiki pages
argument-hint: [optional-filter]
---

# Wiki List

The user invoked this command with: $ARGUMENTS

Use `arcwell wiki list`. If a filter is supplied, search first with `wiki_search`; otherwise summarize recent or relevant page ids and titles.
